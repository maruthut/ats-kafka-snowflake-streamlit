# Snowflake Configuration
SNOWFLAKE_ACCOUNT=your_account.region
SNOWFLAKE_USER=your_username

# IMPORTANT: If password contains $ character, use $$ (double dollar sign) for proper escaping
# Example: If password is "MyPass$word123", set it as "MyPass$$word123"
SNOWFLAKE_PASSWORD=your_password

SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=ATS_DB
SNOWFLAKE_SCHEMA=ATS_SCHEMA
SNOWFLAKE_ROLE=SYSADMIN

# Snowflake Private Key (for Kafka Connector - RSA key-pair authentication)
# Generate using:
#   openssl genrsa -out snowflake_key.pem 2048
#   openssl rsa -in snowflake_key.pem -pubout -out snowflake_key.pub
#   openssl pkcs8 -topk8 -inform PEM -outform PEM -in snowflake_key.pem -out snowflake_key_pkcs8.pem -nocrypt
# Then assign public key to Snowflake user:
#   ALTER USER your_username SET RSA_PUBLIC_KEY='<public_key_content>';
# Paste the PKCS8 private key content below (remove headers/footers, single line):
SNOWFLAKE_PRIVATE_KEY=

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
