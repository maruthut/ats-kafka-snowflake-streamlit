# Snowflake Streamlit Environment Configuration
# For Streamlit-in-Snowflake, specify only the packages you need
# Snowflake automatically manages the Python runtime

name: streamlit
channels:
  - snowflake
dependencies:
  - streamlit
  - snowflake-snowpark-python
  - pandas
  - plotly
